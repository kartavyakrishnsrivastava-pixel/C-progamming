#include <stdio.h>
int main(){

// // variable = a resuable container that stores data values
// behaves as if it is a box that can hold different types of data, such as numbers, characters, or strings. You can assign a value to a variable and then use that variable throughout your program to refer to that value. This allows for more flexible and maintainable code, as you can easily change the value stored in the variable without having to modify every instance where that value is used.


int age = 25;
int year = 2026;
int quantity = 100;
printf("i am %d years old\n", age); // printing the value of 'age' using the %d format specifier for integers//
// 
printf("this year is %d\n", year); // printing the value of 'year' using the %d format specifier for integers//
printf(" she bought %d pizzas\n", quantity); // printing the value of 'quantity' using the %d format specifier for integers//
return 0;
}